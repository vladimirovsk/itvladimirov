//const fs = require('fs'), path = require('path')
global.db = require('./db');

// fs.readdirSync(path.join(__dirname, '/db/data')).forEach(file => {
//     let model = require(path.join(__dirname, '/db/data/'+file))
// });